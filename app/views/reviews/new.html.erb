<br>
<br>
<div class="container new">
  <div class="container title">
    <h1>Add a review</h1>
  </div>
  <div class="container form">
    <%= simple_form_for [@castle, @review] do |f| %>
      <%= f.input :comment %>
      <%= f.input :rating, collection:0..5 %>
      <%= f.submit 'Add your review', class:'addlink' %>
    <% end %>
  </div>
</div>
<div class="back">
  <%= link_to 'Back', castle_path(@castle), class: 'backlink'%>
</div>
